---
import Layout from '@/layouts/Layout.astro'
import { WorkoutDay } from '@/components/workout-day'

const { day_id } = Astro.params
if (!day_id) {
  return Astro.redirect('/')
}
---

<Layout title='Gym App'>
  <WorkoutDay client:load workoutDayId={day_id} />
</Layout>
