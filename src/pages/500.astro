---
import Layout from '@/layouts/Layout.astro'

const { error } = Astro.props
const message = error instanceof Error ? error.message : 'Error interno'

const title = `Fit Drummes | ${message}`
---

<Layout title={title}>
  <p>{message}</p>
</Layout>
